export const PIXELS_PER_SECOND = 60; // zoom level - 60px/sec = ~17ms per pixel (good for minute-level overview)
export const LANE_HEIGHT = 60;
export const AXIS_HEIGHT = 30;
export const SPAN_HEIGHT = 20;
export const TOLERANCE_MS = 250; // 250ms tolerance - good for minute-level work
export const GAP_THRESHOLD_MS = 1800000; // 30 minutes
export const GAP_MARKER_WIDTH = 100; // pixels for gap marker
